// Code generated by pathbind-gen --mode=tf. DO NOT EDIT.
// Source: pathbind-overrides.yaml

package {{.Package}}

import (
	"github.com/hashicorp/terraform-plugin-framework/types"
)

// {{.ResourceName}}State holds Terraform state and plan data for {{.ResourceName}} resources.
// This struct is used for Terraform schema binding (input plan and output state).
// Fields are tagged with tfsdk (Terraform state key) only.
// DO NOT use hfsdk tags here; use {{.ResourceName}}StateNative for pathbind operations.
type {{.ResourceName}}State struct {
{{- range .AllFields}}
	{{.GoName}} {{if hasSuffix .Type "[]"}}types.List{{else if eq .Type "string"}}types.String{{else if eq .Type "*string"}}types.String{{else if eq .Type "bool"}}types.Bool{{else if eq .Type "*bool"}}types.Bool{{else if eq .Type "int32"}}types.Int64{{else if eq .Type "*int32"}}types.Int64{{else if eq .Type "int64"}}types.Int64{{else if eq .Type "*int64"}}types.Int64{{else if eq .Type "map"}}types.Map{{else}}types.String{{end}} `tfsdk:"{{attrName .}}"`
{{- end}}
}

// mergeResponse{{.ResourceName}} merges API response values into the plan state.
// Only computed or required fields are merged from the response. Optional fields
// are skipped to preserve user-provided values and avoid overwriting with API defaults.
func mergeResponse{{.ResourceName}}(plan, response *{{.ResourceName}}State) {
	if response == nil {
		return
	}
	// Only copy computed and required fields from response, skip optional fields
	// to avoid overwriting user inputs with API-generated default values
{{- range .AllFields}}
{{- if or .Computed .Required}}
	if !response.{{.GoName}}.IsNull() {
		plan.{{.GoName}} = response.{{.GoName}}
	}
{{- end}}
{{- end}}
}
